name: onesignal_flutter
description: Local offline stub for the OneSignal Flutter API used by the app.
version: 0.0.1
publish_to: none

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
